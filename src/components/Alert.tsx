// styling:
import { css, jsx } from "@emotion/react";
import styled from "@emotion/styled";

const Container = styled.div``;

type AlertProps = {};

const Alert = ({}: AlertProps) => {
  return <Container role="dialog" aria-labelledby=""></Container>;
};

export default Alert;
